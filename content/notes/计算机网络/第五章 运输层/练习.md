---
title: 第五章 运输层 — 练习
date: 2026-06-02T08:00:00+08:00
tags:
  - 大三下
  - 计算机网络
  - 练习
summary: 第五章运输层选择填空练习题，覆盖全部知识点。
draft: true
---

## 一、选择题

### 5.1 运输层协议概述

1. 运输层向应用层提供（B ）之间的逻辑通信。
   - A. 主机与主机
   - B. 进程与进程
   - C. 路由器与路由器
   - D. 网络与网络

2. 以下关于运输层的描述，正确的是（C ）。
   - A. 路由器中实现了完整的运输层功能
   - B. 运输层属于网络核心的功能
   - C. 运输层向高层屏蔽了网络核心的细节
   - D. 运输层只能提供可靠的通信服务

3. 以下哪个协议是面向连接的？（C ）
   - A. UDP
   - B. IP
   - C. TCP
   - D. ICMP

4. 以下哪个应用层协议通常使用 UDP？（D ）
   - A. HTTP
   - B. SMTP
   - C. FTP
   - D. DNS

5. 关于运输层端口，以下说法错误的是（B ）。
   - A. 端口号用 16 位表示，共 65535 个
   - B. 端口号具有全局意义，互联网中所有计算机的同一端口号对应同一进程
   - C. 熟知端口的范围是 0～1023
   - D. 短暂端口的范围是 49152～65535

6. HTTP 协议使用的熟知端口号是（C ）。
   - A. 21
   - B. 25
   - C. 80
   - D. 443

7. 以下哪个端口号属于登记端口范围？（C ）
   - A. 53
   - B. 80
   - C. 8080
   - D. 60000

### 5.2 UDP

8. UDP 首部的长度是（B ）字节。
   - A. 4
   - B. 8
   - C. 12
   - D. 20

8. 关于 UDP 的特点，以下说法错误的是（C ）。
   - A. 无连接
   - B. 尽最大努力交付
   - C. 有拥塞控制
   - D. 支持一对一、一对多通信

10. UDP 计算检验和时，会在 UDP 用户数据报前面临时添加（C ）。
    - A. IP 首部
    - B. TCP 首部
    - C. 伪首部
    - D. 以太网帧首部

11. UDP 伪首部中协议号字段的值是（C ）。
    - A. 1
    - B. 6
    - C. 17
    - D. 0

12. UDP 检验和计算采用的方法是（B ）。
    - A. 奇偶校验
    - B. CRC 循环冗余校验
    - C. 16 位二进制反码求和
    - D. MD5 校验

13. 当 UDP 检验和计算结果为全 0 时，应如何处理？（B ）
    - A. 直接填入全 0
    - B. 改为全 1 发送
    - C. 重新计算
    - D. 忽略检验和

14. 当接收方发现 UDP 报文的目的端口不可达时，会（B ）。
    - A. 直接丢弃，不发送任何通知
    - B. 丢弃并向发送方发送 ICMP "端口不可达"差错报文
    - C. 将报文转发给其他端口
    - D. 缓存报文等待进程监听

15. 当接收方发现 UDP 检验和出错时，会（A ）。
    - A. 丢弃报文并向发送方发送 ICMP 差错报文
    - B. 丢弃报文，不发送 ICMP 差错报文
    - C. 请求发送方重传
    - D. 尝试修复数据

### 5.3 TCP 概述

16. 关于 TCP 的特点，以下说法正确的是（C ）。
    - A. TCP 是无连接的
    - B. TCP 提供半双工通信
    - C. TCP 是面向字节流的
    - D. TCP 支持广播

17. TCP 连接的端点是（C ）。
    - A. IP 地址
    - B. 端口号
    - C. 套接字（socket）
    - D. MAC 地址

18. 套接字的表示形式是（B ）。
    - A. MAC地址:端口号
    - B. IP地址:端口号
    - C. IP地址:MAC地址
    - D. 主机名:端口号

19. 关于 TCP 面向字节流的特性，以下说法错误的是（B ）。
    - A. TCP 把应用程序交下来的数据看成无结构的字节流
    - B. 接收方应用程序收到的数据块大小一定等于发送方发出的数据块大小
    - C. TCP 不关心应用进程一次发送多长的报文
    - D. TCP 根据窗口值和拥塞程度决定报文段包含多少字节

### 5.4 可靠传输的工作原理

20. 停止等待 ARQ 协议中，发送方解决分组丢失的方法是（B ）。
    - A. 增大发送窗口
    - B. 超时重传
    - C. 减少分组大小
    - D. 增加确认频率

21. 停止等待 ARQ 协议的信道利用率公式中，分母不包含以下哪项？（D ）
    - A. 分组发送时间 $T_D$
    - B. 往返时间 RTT
    - C. 确认发送时间 $T_A$
    - D. 处理时间 $T_P$

22. 连续 ARQ 协议采用的确认方式是（B ）。
    - A. 逐个确认
    - B. 累积确认
    - C. 选择确认
    - D. 否定确认

23. 连续 ARQ 协议中 Go-back-N 的含义是（C ）。
    - A. 只重传丢失的那个分组
    - B. 重传从丢失分组开始及其后已发送的所有分组
    - C. 重传发送窗口内的所有分组
    - D. 从第一个分组开始重新发送

### 5.5 TCP 报文段的首部格式

24. TCP 首部的最小长度是（B ）字节。
    - A. 8
    - B. 20
    - C. 40
    - D. 60

25. TCP 首部中数据偏移字段的单位是（D ）。
    - A. 1 字节
    - B. 2 字节
    - C. 4 字节
    - D. 8 字节

26. TCP 首部中确认号字段的含义是（B ）。
    - A. 已正确收到的最后一个字节的序号
    - B. 期望收到对方下一个报文段的数据的第一个字节的序号
    - C. 本报文段发送数据的第一个字节的序号
    - D. 本报文段发送数据的最后一个字节的序号

27. 以下哪个控制位用于连接建立？（C ）
    - A. FIN
    - B. RST
    - C. SYN
    - D. PSH

28. 当 TCP 报文段中 $SYN=1, ACK=0$ 时，表示（B ）。
    - A. 连接接受报文段
    - B. 连接请求报文段
    - C. 连接释放报文段
    - D. 数据确认报文段

29. MSS（最大报文段长度）指的是（C ）。
    - A. TCP 报文段的总长度最大值
    - B. TCP 首部长度的最大值
    - C. TCP 报文段中数据字段的最大长度
    - D. 接收窗口的最大值

30. 已知以太网的 MTU 为 1500 字节，IP 首部和 TCP 首部均为 20 字节，则 MSS 应为（C ）字节。
    - A. 1500
    - B. 1480
    - C. 1460
    - D. 1440

### 5.6 TCP 可靠传输的实现

31. TCP 滑动窗口的单位是（A ）。
    - A. 分组
    - B. 报文段
    - C. 字节
    - D. 帧

32. 假设发送窗口后沿为 $P_1$，发送窗口前沿为 $P_3$，则 $P_3-P_1$ 表示（C ）。
    - A. 已发送但未收到确认的字节数
    - B. 允许发送但尚未发送的字节数
    - C. 发送窗口大小
    - D. 已收到确认的字节数

33. TCP 的确认方式是（B ）。
    - A. 选择确认
    - B. 累积确认
    - C. 否定确认
    - D. 不确认

34. 若接收方收到序号 32、33 的数据，但未收到序号 31 的数据，则其发送的确认号是（C ）。
    - A. 33
    - B. 34
    - C. 31
    - D. 32

35. Karn 算法的核心思想是（B ）。
    - A. 只使用重传报文段的 RTT 样本
    - B. 只要报文段重传了，就不采用其往返时间样本
    - C. 每次重传后将 RTO 减半
    - D. 只使用首次发送的报文段的 RTT 样本

36. 修正的 Karn 算法中，报文段每重传一次，新的 RTO 等于（B ）。
    - A. 旧的 RTO / 2
    - B. 旧的 RTO × 2
    - C. 旧的 RTO + 1
    - D. 旧的 RTO 不变

37. 选择确认 SACK 的作用是（B ）。
    - A. 替代累积确认
    - B. 只重传缺少的数据而不重传已正确到达的数据
    - C. 取消超时重传机制
    - D. 扩大接收窗口

### 5.7 TCP 的流量控制

38. TCP 流量控制的目的在于（B ）。
    - A. 防止网络拥塞
    - B. 让发送方发送速率不要太快，使接收方来得及接收
    - C. 提高信道利用率
    - D. 减小传输时延

39. 当接收方发送 $rwnd=0$ 后，发送方如何得知接收方窗口又变为非零？（B ）
    - A. 接收方主动发送窗口更新通知
    - B. 发送方通过持续计时器发送零窗口探测报文段
    - C. 发送方周期性重传数据
    - D. 发送方使用保活计时器

40. 发送方产生糊涂窗口综合症的原因是（A ）。
    - A. 发送方每次接收到 1 字节数据后就立即发送
    - B. 发送方窗口设置过大
    - C. 发送方拥塞窗口增长过快
    - D. 发送方超时计时器设置过短

41. Nagle 算法用于解决（C ）。
    - A. 网络拥塞
    - B. 发送方糊涂窗口综合症
    - C. 接收方糊涂窗口综合症
    - D. 连接建立失败

### 5.8 TCP 的拥塞控制

42. 拥塞控制与流量控制的区别在于（A ）。
    - A. 拥塞控制是全局性的，流量控制是点对点的
    - B. 拥塞控制是点对点的，流量控制是全局性的
    - C. 两者完全等同
    - D. 拥塞控制由接收方执行，流量控制由发送方执行

43. TCP 发送窗口的上限值由（B ）决定。
    - A. $\max(rwnd, cwnd)$
    - B. $\min(rwnd, cwnd)$
    - C. $rwnd + cwnd$
    - D. $rwnd \times cwnd$

44. 发送方通过（B ）判断网络已出现拥塞。
    - A. 收到重复确认
    - B. 超时重传计时器超时
    - C. 接收窗口变小
    - D. 收到零窗口通知

45. 慢开始算法中，拥塞窗口的初始值通常为（B ）。
    - A. 0
    - B. 1 个报文段
    - C. 2 个报文段
    - D. ssthresh 的值

46. 慢开始阶段，拥塞窗口的增长方式是（B ）。
    - A. 线性增长
    - B. 指数增长
    - C. 保持不变
    - D. 逐渐减小

47. 拥塞避免阶段，每经过一个 RTT，拥塞窗口增加（A ）。
    - A. 1
    - B. 2
    - C. 当前窗口值的一半
    - D. 指数增长

48. 当 $cwnd < ssthresh$ 时，TCP 使用（B ）。
    - A. 拥塞避免算法
    - B. 慢开始算法
    - C. 快恢复算法
    - D. 快重传算法

49. 快重传算法中，发送方收到几个重复确认后立即重传？（C ）
    - A. 1 个
    - B. 2 个
    - C. 3 个
    - D. 4 个

50. 快恢复算法中，当收到 3 个重复确认后，$cwnd$ 变为（B ）。
    - A. 1
    - B. 当前 $cwnd$ 的一半
    - C. 保持不变
    - D. 初始值

51. 当发生超时重传时，$cwnd$ 变为（C ）。
    - A. 保持不变
    - B. 减半
    - C. 变为 1
    - D. 变为 ssthresh

52. 全局同步现象是由以下哪种策略引起的？（B ）
    - A. 主动队列管理 AQM
    - B. 尾部丢弃策略
    - C. 随机早期检测 RED
    - D. 快重传

53. 关于 RED（随机早期检测），当平均队列长度 $L_{AV}>Th_{max}$ 时，丢弃概率为（C ）。
    - A. 0
    - B. 0.5
    - C. 1
    - D. 随机值

### 5.9 TCP 的运输连接管理

54. TCP 连接建立采用（B ）。
    - A. 两次握手
    - B. 三报文握手
    - C. 四报文握手
    - D. 一次请求即可

55. TCP 连接释放采用（C ）。
    - A. 两次挥手
    - B. 三报文握手
    - C. 四报文握手
    - D. 直接断开

56. 在 TCP 三次握手中，第二次握手的报文段标志位为（B ）。
    - A. $SYN=1, ACK=0$
    - B. $SYN=1, ACK=1$
    - C. $SYN=0, ACK=1$
    - D. $FIN=1, ACK=1$

57. SYN 报文段携带数据时，是否消耗序号？（D ）
    - A. 不消耗序号
    - B. 消耗一个序号
    - C. SYN 报文段可以携带数据
    - D. SYN 报文段不可携带数据，但要消耗一个序号

58. 主动关闭方在发送最后一个 ACK 后，必须等待（C ）才能释放连接。
    - A. 1 个 RTT
    - B. 1 个 MSL
    - C. 2 个 MSL
    - D. 4 个 MSL

59. 保活计时器的典型初始值是（C ）。
    - A. 30 秒
    - B. 2 分钟
    - C. 2 小时
    - D. 24 小时

60. TCP 三次握手中，第三次握手的报文段通常的标志位是（C ）。
    - A. $SYN=1, ACK=0$
    - B. $SYN=1, ACK=1$
    - C. $ACK=1$
    - D. $FIN=1$

---

## 二、填空题

### 5.1 运输层协议概述

1. 运输层为 \_\_\_\_网络进程\_\_\_\_ 之间的通信提供服务，网络层为 \_\_\_\_两台主机\_\_\_\_ 之间的通信提供服务。

2. TCP 传送的数据单位称为 \_\_\_\_TCP报文段\_\_\_\_ ，UDP 传送的数据单位称为 \_\_\_\_UDP报文\_\_\_\_ 或用户数据报。

3. 运输层的两个主要协议是 \_\_\_\_TCP协议\_\_\_\_ 和 \_\_\_\_UDP协议\_\_\_\_。

4. TCP 不提供 \_\_\_\_一对多\_\_\_\_ 和 \_\_\_\_多对多\_\_\_\_ 服务。

5. 端口号用 \_\_\_\_16\_\_\_\_ 位表示，共有 \_\_\_\_65535\_\_\_\_ 个端口号，分为三大类：\_\_\_\_固定端口号\_\_\_\_、\_\_\_\_注册端口号\_\_\_\_ 和 \_\_\_\_短暂端口号\_\_\_\_。

6. FTP 的熟知端口号是 \_\_\_\_\_\_\_\_，SMTP 的是 \_\_\_\_\_\_\_\_，DNS 的是 \_\_\_\_\_\_\_\_，HTTP 的是 \_\_\_\_\_\_\_\_，HTTPS 的是 \_\_\_\_\_\_\_\_。

### 5.2 UDP

7. UDP 只在 IP 数据报服务之上增加了 \_\_\_\_端口号\_\_\_\_ 和 \_\_\_\_差错检测\_\_\_\_ 功能。

8. UDP 是 \_\_\_\_无连接\_\_\_\_ 的协议，发送数据之前不需要 \_\_\_\_建立连接\_\_\_\_。

9. UDP 的首部共 \_\_\_\_8\_\_\_\_ 字节，由 \_\_\_\_4\_\_\_\_ 个字段组成，每个字段均为 \_\_\_\_2\_\_\_\_ 字节。

10. UDP 是面向 \_\_\_\_报文段\_\_\_\_ 的协议，一次传送和交付一个 \_\_\_\_完整\_\_\_\_ 的报文，既不 \_\_\_\_拆分\_\_\_\_ 也不 \_\_\_\_整合\_\_\_\_。

11. UDP 伪首部包含的五个字段是：\_\_\_\_源IP地址\_\_\_\_、\_\_\_\_目的IP地址\_\_\_\_、\_\_\_\_？\_\_\_\_、\_\_\_\_？\_\_\_\_、\_\_\_\_？\_\_\_\_。伪首部的总长度为 \_\_\_\_20\_\_\_\_ 字节。

12. UDP 伪首部中协议号的值为 \_\_\_\_\_\_\_\_，全 0 字段占 \_\_\_\_\_\_\_\_ 字节。

13. UDP 采用 \_\_\_\_16位二进制反码求和\_\_\_\_ 计算检验和，检验范围包括 \_\_\_\_伪首部\_\_\_\_ 和 \_\_\_\_UDP数据报\_\_\_\_ 两部分。

14. 若 UDP 检验和计算结果是全 0，必须将其改为 \_\_\_\_全1\_\_\_\_ 再发送，因为在反码算术中，\_\_\_\_全1\_\_\_\_ 和 \_\_\_\_全0\_\_\_\_ 数学意义等价。

### 5.3 TCP 概述

15. TCP 提供的通信模式是 \_\_\_\_全\_\_\_\_ 双工通信。

16. TCP 是面向 \_\_\_\_字节\_\_\_\_ 的协议，把应用程序交下来的数据看作无结构的 \_\_\_\_字节串\_\_\_\_。

17. TCP 连接的端点是 \_\_\_\_套接字\_\_\_\_，其表达式为 \_\_\_\_IP地址加端口号\_\_\_\_。

18. 每一条 TCP 连接唯一地被通信两端的 \_\_\_\_两\_\_\_\_ 个端点确定。

### 5.4 可靠传输的工作原理

19. 停止等待 ARQ 协议中，发送方解决分组差错或丢失的机制是 \_\_\_\_丢失重传\_\_\_\_。

20. 停止等待协议的信道利用率公式为 $U=$ \_\_\_\_\_\_\_\_。

21. 连续 ARQ 协议采用 \_\_\_\_滑动窗口\_\_\_\_ 传输和 \_\_\_\_累计\_\_\_\_ 确认机制。

22. 连续 ARQ 协议采用 \_\_\_\_Go-back-n\_\_\_\_（回退 N）策略来重传丢失的分组。

### 5.5 TCP 报文段的首部格式

23. TCP 报文段由 \_\_\_\_TCP首部\_\_\_\_ 和 \_\_\_\_报文段\_\_\_\_ 两部分组成。TCP 首部前 \_\_\_\_20\_\_\_\_ 个字节是固定的，首部最小长度为 \_\_\_\_20\_\_\_\_ 字节。

24. 序号字段占 \_\_\_\_4\_\_\_\_ 字节，其值指本报文段所发送数据的 \_\_\_\_第一个字节\_\_\_\_ 的序号。

25. 确认号字段占 \_\_\_\_4\_\_\_\_ 字节，若确认号 = N，则表示到序号 \_\_\_\_N-1\_\_\_\_ 为止的所有数据都已正确收到。

26. 数据偏移字段占 \_\_\_\_4\_\_\_\_ 位，单位是 \_\_\_\_4\_\_\_\_ 字节，其最小值为 \_\_\_\_5\_\_\_\_，最大值为 \_\_\_\_15\_\_\_\_。

27. TCP 首部中，\textbf{确认号}负责确认滑动窗口的 \_\_\_\_后沿\_\_\_\_（后沿），而 \textbf{窗口值}负责确认滑动窗口的 \_\_\_\_前沿\_\_\_\_（前沿）。

28. 写出以下控制位的含义：URG：\_\_\_\_紧急\_\_\_\_、ACK：\_\_\_\_应答\_\_\_\_、PSH：\_\_\_\_推送\_\_\_\_、RST：\_\_\_\_重置\_\_\_\_、SYN：\_\_\_\_同步\_\_\_\_、FIN：\_\_\_\_结束\_\_\_\_。

29. 紧急指针字段仅在 \_\_\_\_出现错误\_\_\_\_ 时有效。

30. MSS 是 TCP 报文段中 \_\_\_\_数据段\_\_\_\_ 的最大长度。MSS = \_\_\_\_TCP数据报的总长度\_\_\_\_ − \_\_\_\_TCP首部长度\_\_\_\_。

31. MSS 选择的原则是：尽可能 \_\_\_\_大\_\_\_\_，但要保证在 IP 层传输时不再 \_\_\_\_分片\_\_\_\_。

### 5.6 TCP 可靠传输的实现

32. TCP 的滑动窗口是以 \_\_\_\_字节\_\_\_\_ 为单位的。

33. 设 $P_1$ 为发送窗口后沿，$P_2$ 为当前发送位置，$P_3$ 为发送窗口前沿，则：
    - 发送窗口大小 = \_\_\_\_P3-P1\_\_\_\_
    - 已发送但尚未收到确认的字节数 = \_\_\_\_P2-P1\_\_\_\_
    - 可用窗口（允许发送但尚未发送） = \_\_\_\_P3-P2\_\_\_\_

34. TCP 发送缓存中存放的两类数据是：\_\_\_\_发送窗口数据\_\_\_\_ 和 \_\_\_\_不允许发送的数据\_\_\_\_。

35. 首次测量 RTT 时：$RTT_S=$ \_\_\_\_\_\_\_\_，$RTT_D=$ \_\_\_\_\_\_\_\_，$RTO=$ \_\_\_\_\_\_\_\_。

36. Karn 算法的核心思想是：只要报文段 \_\_\_\_重传\_\_\_\_ 了，就不采用其 \_\_\_\_RTTs\_\_\_\_ 样本。

37. 修正的 Karn 算法中，报文段每重传一次，新的 $RTO=$ \_\_\_\_2\_\_\_\_ × 旧的 $RTO$，$\gamma$ 的典型值为 \_\_\_\_\_\_\_\_。

38. SACK 选项中，左边界 = \_\_\_\_\_\_\_\_，右边界 = \_\_\_\_\_\_\_\_。

### 5.7 TCP 的流量控制

39. 流量控制的目的，是让 \_\_\_\_发送方\_\_\_\_ 的发送速率不要太快，使 \_\_\_\_接收方\_\_\_\_ 来得及接收。

40. 当接收方发送零窗口通知后，发送方使用 \_\_\_\_计时器\_\_\_\_ 来避免死锁，其发送的探测报文段携带 \_\_\_\_0\_\_\_\_ 字节数据。

41. 糊涂窗口综合症是指每次仅发送 \_\_\_\_1\_\_\_\_ 字节或很少几个字节数据，有效传输效率很 \_\_\_\_低\_\_\_\_。

42. 解决发送方糊涂窗口综合症的方法是 \_\_\_\_Nagle\_\_\_\_ 算法。

43. 解决接收方糊涂窗口综合症的方法是让接收方等待一段时间，直到接收缓存有 \_\_\_\_\_\_\_\_ 容纳一个最长报文段的空间，或等到接收缓存有 \_\_\_\_\_\_\_\_ 空闲空间。

### 5.8 TCP 的拥塞控制

44. 出现网络拥塞的条件是：$\sum$ \_\_\_\_\_\_\_\_ > \_\_\_\_\_\_\_\_。

45. 拥塞控制是 \_\_\_\_全局\_\_\_\_ 性的过程，流量控制是 \_\_\_\_局部\_\_\_\_ 的问题。

46. 拥塞控制的两种思路是 \_\_\_\_开环\_\_\_\_ 控制和 \_\_\_\_闭环\_\_\_\_ 控制，TCP 采用 \_\_\_\_闭环\_\_\_\_ 控制方法。

47. TCP 发送窗口的上限值 = \_\_\_\_\_\_\_\_。

48. TCP 拥塞控制的四种核心算法是：\_\_\_\_慢开始\_\_\_\_、\_\_\_\_拥塞避免\_\_\_\_、\_\_\_\_快恢复\_\_\_\_、\_\_\_\_快重传\_\_\_\_。

49. 慢开始门限 $ssthresh$ 的作用：当 $cwnd < ssthresh$ 时使用 \_\_\_\_慢开始\_\_\_\_ 算法；当 $cwnd > ssthresh$ 时使用 \_\_\_\_拥塞避免\_\_\_\_ 算法。

50. 超时重传时，$ssthresh$ 变为 \_\_\_\_cwnd的一半\_\_\_\_，$cwnd$ 变为 \_\_\_\_1\_\_\_\_，然后执行 \_\_\_\_慢开始\_\_\_\_。

51. 收到 3 个重复 ACK 时，$ssthresh$ 变为 \_\_\_\_cwnd的一半\_\_\_\_，$cwnd$ 变为 \_\_\_\_cwnd的一半\_\_\_\_，然后执行 \_\_\_\_快重传和快恢复\_\_\_\_。

52. AQM 的全称是 \_\_\_\_主动队列管理\_\_\_\_，其思想是不要等到队列满了再丢弃，而是在队列长度达到警戒值时 \_\_\_\_自动\_\_\_\_ 丢弃分组。

53. RED 路由器中，当平均队列长度 $L_{AV} < Th_{min}$ 时丢弃概率 $p=$ \_\_\_\_0\_\_\_\_；当 $L_{AV} > Th_{max}$ 时 $p=$ \_\_\_\_1\_\_\_\_；当 $Th_{min} \le L_{AV} \le Th_{max}$ 时 $p$ 在 \_\_\_\_0\_\_\_\_ 到 \_\_\_\_1\_\_\_\_ 之间。

### 5.9 TCP 的运输连接管理

54. TCP 连接建立的三个阶段是：\_\_\_\_SYN\_\_\_\_、\_\_\_\_SYN ACK\_\_\_\_、\_\_\_\_ACK\_\_\_\_。

55. TCP 采用 \_\_\_\_三次\_\_\_\_ 报文握手建立连接，采用 \_\_\_\_四次\_\_\_\_ 报文挥手释放连接。

56. 第三次握手中，ACK 报文段若不携带数据，则 \_\_\_\_不\_\_\_\_ 消耗序号，下一个数据报文段的序号仍是 \_\_\_\_本身\_\_\_\_。

57. 三报文握手时，第一次握手报文段中 $SYN=$ \_\_\_\_1\_\_\_\_；第二次握手报文段中 $SYN=$ \_\_\_\_1\_\_\_\_、$ACK=$ \_\_\_\_1\_\_\_\_；第三次握手报文段中 $ACK=$ \_\_\_\_1\_\_\_\_。

58. 主动关闭方发送最后一个 ACK 后，必须等待 \_\_\_\_一段时间\_\_\_\_ 才能完全释放连接，等待时间至少为 \_\_\_\_2MSL\_\_\_\_。

59. 必须等待 2MSL 的两个原因：
    - (1) \_\_\_\_保证 A 发送的最后一个 ACK 报文段能够到达 B。若这个 ACK 丢失，B 会重传 FIN，A 还能够再次发送 ACK。\_\_\_\_
    - (2) \_\_\_\_防止已失效的连接请求报文段（特指三次握手时的 **第一次握手**）出现在本连接中。\_\_\_\_

60. 保活计时器通常设置为 \_\_\_\_2\_\_\_\_ 小时，若服务器在此期间未收到客户端信息，就发送探测报文段。最多发送 \_\_\_\_10\_\_\_\_ 个探测报文段，每个相隔 \_\_\_\_75\_\_\_\_ 秒。

---

## 三、综合计算与应用题

1. 主机 A 向主机 B 发送 5000 字节的数据，TCP 报文段最大数据长度为 1000 字节，初始序号为 1001。写出各报文段的序号和数据字节序号范围。

2. 主机 A 收到 B 发来的 TCP 报文段，确认号为 2001。问：到序号多少为止的数据已正确收到？

3. 一个 TCP 连接初始化时 $cwnd=1$，$ssthresh=16$。经历了慢开始和拥塞避免阶段，当 $cwnd=24$ 时发生超时重传。请写出：
   - (1) 超时后新的 $ssthresh$ 和 $cwnd$。
   - (2) 重新进入慢开始后，什么时候转为拥塞避免？

4. 一个 TCP 连接当前 $cwnd=16$，$ssthresh=8$，此时收到 3 个重复 ACK。请写出执行快重传和快恢复后新的 $ssthresh$ 和 $cwnd$ 的值。

5. 假设接收方收到了两个不连续的字节块：$1501 \sim 3000$ 和 $3501 \sim 4500$，而序号 $1001 \sim 1500$ 尚未收到。请写出：
   - (1) 累积确认号是多少？
   - (2) SACK 选项报告的边界 $L_1, R_1, L_2, R_2$ 各是多少？

6. 已知以太网 MTU = 1500 字节，IP 首部为 20 字节，TCP 首部为 20 字节，求 MSS 的值。

---

## 参考答案

### 选择题答案

| 题号 | 答案 | 题号 | 答案 | 题号 | 答案 | 题号 | 答案 |
|------|------|------|------|------|------|------|------|
| 1 | B | 16 | C | 31 | C | 46 | B |
| 2 | C | 17 | C | 32 | C | 47 | A |
| 3 | C | 18 | B | 33 | B | 48 | B |
| 4 | D | 19 | B | 34 | C | 49 | C |
| 5 | B | 20 | B | 35 | B | 50 | B |
| 6 | C | 21 | D | 36 | B | 51 | C |
| 7 | C | 22 | B | 37 | B | 52 | B |
| 8 | B | 23 | B | 38 | B | 53 | C |
| 9 | C | 24 | B | 39 | B | 54 | B |
| 10 | C | 25 | C | 40 | A | 55 | C |
| 11 | C | 26 | B | 41 | B | 56 | B |
| 12 | C | 27 | C | 42 | A | 57 | D |
| 13 | B | 28 | B | 43 | B | 58 | C |
| 14 | B | 29 | C | 44 | B | 59 | C |
| 15 | B | 30 | C | 45 | B | 60 | C |

### 填空题答案

1. 应用进程 / 主机
2. TCP 报文段 / UDP 报文
3. TCP / UDP
4. 广播 / 多播
5. 16 / 65535 / 熟知端口(0~1023) / 登记端口(1024~49151) / 短暂端口(49152~65535)
6. 21 / 25 / 53 / 80 / 443
7. 复用和分用 / 差错检测
8. 无连接 / 建立连接
9. 8 / 4 / 2
10. 报文 / 完整 / 合并 / 拆分
11. 源IP地址 / 目的IP地址 / 全0 / 协议号 / UDP长度 / 12
12. 17 / 1
13. 16位二进制反码求和 / 首部 / 数据
14. 全1(FF FF) / +0 / -0
15. 全
16. 字节流 / 字节流
17. 套接字(socket) / IP地址:端口号
18. 两
19. 超时重传
20. $U=\frac{T_D}{T_D+RTT+T_A}$
21. 流水线 / 累积
22. Go-back-N
23. 首部 / 数据 / 20 / 20
24. 4 / 第一个字节
25. 4 / N-1
26. 4 / 4 / 5 / 15
27. 左边界 / 右边界
28. 紧急 / 确认 / 推送 / 复位 / 同步 / 终止
29. $URG=1$
30. 数据字段 / TCP报文段长度 / TCP首部长度
31. 大 / 分片
32. 字节
33. $P_3-P_1$ / $P_2-P_1$ / $P_3-P_2$
34. 准备发送的数据 / 已发送但尚未收到确认的数据
35. $RTT_{new}$ / $RTT_{new}/2$ / $RTT_S+4\times RTT_D$
36. 重传 / 往返时间(RTT)
37. $\gamma$ / 2
38. 字节块第一个字节的序号 / 字节块最后一个字节的序号+1
39. 发送方 / 接收方
40. 持续计时器 / 1
41. 1 / 低
42. Nagle
43. 足够空间 / 一半
44. 对资源的需求 / 可用资源
45. 全局 / 端到端（点对点）
46. 开环 / 闭环 / 闭环
47. $\min(rwnd, cwnd)$
48. 慢开始 / 拥塞避免 / 快重传 / 快恢复
49. 慢开始 / 拥塞避免
50. $\max(cwnd/2, 2)$（或当前 $cwnd$ 的一半） / 1 / 慢开始
51. $cwnd/2$ / $cwnd/2$（即新的 $ssthresh$） / 拥塞避免（快恢复）
52. 主动队列管理 / 主动
53. 0 / 1 / 0 / 1
54. 连接建立 / 数据传送 / 连接释放
55. 三 / 四
56. 不 / $seq=x+1$
57. 1 / 1 / 1 / 1
58. TIME-WAIT 状态（时间等待计时器） / 2MSL
59. 保证最后一个ACK能到达B / 防止已失效的连接请求报文段出现在本连接中
60. 2 / 10 / 75

### 综合计算与应用题答案

1.

| 报文段 | 序号 | 数据字节序号范围 |
|--------|------|-----------------|
| 报文段1 | 1001 | 1001～2000 |
| 报文段2 | 2001 | 2001～3000 |
| 报文段3 | 3001 | 3001～4000 |
| 报文段4 | 4001 | 4001～5000 |
| 报文段5 | 5001 | 5001～6000 |

2. 到序号 2000 为止的数据已正确收到。

3.
- (1) $ssthresh = 12$，$cwnd = 1$。
- (2) 重新慢开始增长，当 $cwnd = 12$（等于 $ssthresh$）时转为拥塞避免。

4. $ssthresh = 8$，$cwnd = 8$，然后执行拥塞避免（线性增长）。

5.
- (1) 累积确认号 = 1001
- (2) $L_1=1501$, $R_1=3001$, $L_2=3501$, $R_2=4501$

6. $MSS = 1500 - 20 - 20 = 1460$ 字节。
