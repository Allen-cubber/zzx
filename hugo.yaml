baseURL: 'https://example.org/'
languageCode: 'zh-cn'
title: 'Z2X BLOG'
theme: 'papermod'

# 启用不安全渲染，让 KaTeX 脚本能工作
markup:
  goldmark:
    renderer:
      unsafe: true

# 主题参数
params:
  # 这一行是全局开关，告诉主题“网站需要数学公式功能”
  math: true